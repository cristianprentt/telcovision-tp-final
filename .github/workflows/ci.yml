name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Say hello
        run: echo "CI listo para TelcoVision"
